namespace chocolatey.infrastructure.app.domain
{
    public enum SourceCommandType
    {
        unknown,
        list,
        add,
        remove,
        enable,
        disable,
    }
}